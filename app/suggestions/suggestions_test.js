'use strict';

describe('suggestions module', function() {

  beforeEach(module('suggestions'));

  describe('suggestions controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var suggestionsCtrl = $controller('SuggestionsViewCtrl');
      expect(SuggestionsViewCtrl).toBeDefined();
    }));

  });
});